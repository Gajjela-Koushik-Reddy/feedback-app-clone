import React from "react";
import { Link } from "react-router-dom";

const Header = () => {
  return (
    <header style={{ color: "#ff6a95", backgroundColor: "rgba(0,0,0,0.4)" }}>
      <div className="container">
        <h2>Feedback App</h2>
      </div>
    </header>
  );
};

export default Header;
